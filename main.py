from html_writer import *

# Our code is inside html_writer.
# It then imports the other files to get the nessesary data.
# In the end a index.html file is created inside /output/index.hml
# We have a html-build.css file as well, to give some colours and sizes to the html
writeHTML()
